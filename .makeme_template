CXX = g++
CXXFLAGS = -std=c++20 -Wall -Wextra -O0

all: main

main: main.cpp
	$(CXX) $(CXXFLAGS) main.cpp -o main

debug:
	$(CXX) --debug $(CXXFLAGS) main.cpp -o debugged

valgrind:
	    valgrind --leak-check=full --show-leak-kinds=all --track-origins=yes ./main

clean:
	rm -f main debugged
